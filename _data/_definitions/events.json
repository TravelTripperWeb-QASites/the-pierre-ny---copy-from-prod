{
  "title": {
    "label": "Category Name",
    "required": true,
    "hint": "Enter event categroy name",
    "localizable": true
  },
  "category_id": {
    "label": "Category Id",
    "required": true,
    "hint": "Should be in lowercase without spaces"
  },
  "category_intro": {
    "label": "Category Intro",
    "required": true,
    "hint": "Short description of event category here",
    "localizable": true
  },
  "category_image": {
    "label": "Category Image",
    "required": true,
    "type": "image"
  },
  "sort_order": {
    "type": "integer",
    "required": true,
    "hint": "Enter sort order for sub categories"
  },
  "event_listing": {
    "type": "nested",
    "multiple": true,
    "localizable": true,
    "hint": "Enter events in this category. Click on 'Add New Menu Items' below to add more events.",
    "fields": {
      "event_name": {
        "required": true
      },
      "event_id": {
        "required": true,
        "hint": "Should be in lowercase without spaces"
      },
      "event_detail": {
        "required": true
      },
      "day": {
        "required": true
      },
      "date": {
        "type": "date",
        "Instructions": "Enter either date or time. If time's given, then date won't be visible on site."
      },
      "time": {
        "Instructions": "Enter either date or time. If time's given, then date won't be visible on site."
      },

      "event_contact": {
        "required": true,
        "type": "richtext",
        "hint": "Please add text with urls if any. To do so, get into code mode."
      },
      "event_booking_id": {
        "required": true,
        "hint": "Enter booking id from OpentTable widget here"
      },
      "view_menu_url": {
        "hint": "Enter menu url here"
      }
    }
  }
}
